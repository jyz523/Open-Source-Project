-- Create the table
CREATE TABLE character_type (
type_id INT PRIMARY KEY,
type_name VARCHAR(50),
health_points INT
);

-- Insert data for character
INSERT INTO character_type (type_id, type_name, health_points)
VALUES
(1, 'Warrior', 15),
(2, 'Archer', 20),
(3, 'Mage', 10),
(4, 'Assassin', 25);
